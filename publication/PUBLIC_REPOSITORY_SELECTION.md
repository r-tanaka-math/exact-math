# Clean public repository candidate

The selected public source for `r-tanaka-math/exact-math` contains the Astro source and lockfile, exactly two registered report PDFs, neutral Workbench presentation files, citation metadata, notices and licenses, and the pinned manual Pages workflow. Its independent `main` history begins with one initial commit; the exact commit and tree are recorded in the release handoff.

The selection excludes the private website Git history, dispatch and Return ZIPs, editable Brief working source, raw prior-art audit, earlier review-labelled Workbench evidence, secrets and owner act, caches, and source maps. Every file under `assets/reports/` must match `publication/pdf-selection.json` by path, size, and SHA-256. Public release requires a separate exact owner act before the first content-bearing push.
